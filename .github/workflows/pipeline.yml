name: Sample

on:
  push:
    branches:
      - main

jobs:
  sample:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "Building Application"
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Test run"
